# substrate-guard v13.2.13 — audit + docker hotfixes

Two no-risk fixes that land cleanly on top of v13.2.12.

- **fix(audit)**: `parse_json_field` now always returns a `dict`, never a bare string or `None`. Fuzz test tightened to match the contract (#5, `9b6253c`).
- **fix(docker)**: Dockerfile now `COPY`s `bandit.yaml` and `examples/` so the in-container pytest suite passes end-to-end (#6, `d6d6edc`).

No API changes, no policy changes, no schema changes. The AST benchmark corpus is unaffected — running `scripts/verify_live.py --update --version v13.2.13` only swaps the `observed.version` label.
