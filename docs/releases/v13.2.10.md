# substrate-guard v13.2.10 — JSON / YAML structural scan

## Modul

- **`substrate_guard/ast_parse/json_yaml_patterns.py`**
  - **JSON:** `json.loads` + parcurgere — chei `__proto__`, `constructor`, `prototype`; limită adâncime.
  - **YAML:** `yaml.safe_load` (PyYAML); în sursă, prezență `!!python` / `!!python/` → încălcare.

## Detector

- **`parser._looks_like_yaml`**: `---`, `%YAML`, sau primul rând stil `key:` (fără a lua JSON `{`/`[`).

## Dependențe dev

- **`pyyaml>=6.0`**

## Teste

- **`tests/test_ast_parse.py`** — detectare limbaj, `__proto__`, tag YAML.
