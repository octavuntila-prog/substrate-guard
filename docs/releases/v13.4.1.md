# v13.4.1 -- Honesty patch (compliance + audit claims)

**Released:** 2 iunie 2026
**Class:** Patch -- zero behavior change (string/wording + defensive only).
No new baseline window required (M0.7 closed; deploy-safe mid-operation).

## What changed

### Honesty cluster (189cf95) -- production-vs-claims accuracy
Code audit (verify-din-cod) found compliance exports + audit summary
overclaimed the production stack. Corrected to reflect reality:
- **compliance.py** (SOC2 / ISO27001 / ISO42001 / summary): L1
  "eBPF Active" -> mock reality (DB-batch; eBPF wired in `monitor --live`,
  not in cron -- correct architecture); L2 "OPA/Rego" -> built-in Python;
  L3 "135 test cases" -> "5 benchmark scenarios, 100%" (unfounded number
  dropped, not swapped per REGULA 0).
- **audit.py**: layers summary observe/policy now derived (not
  hardcoded); run tagline + docstring honest for the batch path.

### W.2/W.3 (2e6922c)
- Chain export extension `.jsonl` -> `.json` (content is pretty-printed
  JSON, not line-delimited).
- Guard init log policy label now dynamic (builtin/OPA).

### #52 (72456fa)
- PolicyEngine warns on empty .rego directory fallback.

## Architecture note (#38b reframe)
eBPF IS wired -- in the live path (`monitor --live`). The cron is
batch-DB audit and correctly uses mock (no live process to observe).
"Layer 1 mock in cron" is the correct architecture, not a gap.

## Rollback
Server: revert /opt/substrate-guard to v13.4.0 code (tag v13.4.0).
Behavior identical; only generated-string content differs.

## Verification
Tests: 398 passed, 7 skipped. Deploy-risk: 0 downstream consumers of
changed strings (cron-audit.sh reads audit_*.json violations/events,
untouched).
