# substrate-guard v13.2.9 — structural SQL + `structural_scan` API

## API

- **`structural_scan(code)`** — intrare unificată: bash, Python, **SQL**.
- **`check_shell_command_ast`** — alias spre `structural_scan`.

## SQL

- **`substrate_guard/ast_parse/sql_patterns.py`** — `sqlparse`: `DROP`, `TRUNCATE`, `ALTER` cu `DROP COLUMN` / `DROP CONSTRAINT`.
- **`detect_shell_language`**: include `WITH `, `TRUNCATE `.

## Dependențe dev

- **`sqlparse>=0.5.0`**
