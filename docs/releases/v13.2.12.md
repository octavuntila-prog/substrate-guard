# substrate-guard v13.2.12 — sqlparse required for SQL structural scan

**`sqlparse>=0.5.0`** moved from optional **dev** to **core** `dependencies`, alongside **PyYAML**.

`sql_destructive_statements` imports `sqlparse` at module load (no silent empty fallback when missing).
