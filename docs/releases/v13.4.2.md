# v13.4.2 — 2026-06-14

Consolidates 75 commits since v13.4.1 (c48d709..146da48), fast-forward
clean, zero divergence. ~80% test-suite depth, ~20% source hardening.
This release ADDS hardening to the builtin policy path (path-canonicalization
and SSRF/metadata-IP denials were NOT present in v13.4.1) and corrects
compliance-report honesty. Builtin remains the production default; the
Research cron runs batch-mock, so these inputs were not on its live
execution path — the hardening is now in place regardless.

## Source hardening (builtin policy path)
- Path-canonicalization and SSRF/metadata-IP denials added to the builtin
  policy evaluator (engine.py). Note: Research cron runs batch-mock; these
  inputs are not on its live execution path. Hardening is now in place
  regardless.
- Type-confusion guards on policy input dispatch.
- `--policy rego` gated as EXPERIMENTAL: PolicyEngine emits a runtime warning
  when the OPA/Rego path makes real decisions. Builtin remains the production
  reference. The Rego path is functional-but-unguaranteed (documented
  divergences on PII / command-denylist / network); not at parity by design.
  See the agent_safety.rego header caveat.

## Ops robustness (audit.py)
- Distinct exit codes for crash vs empty-driver edge cases (preventive;
  not observed in 20+ production nights — Telegram silent throughout).
- ASCII-locale safety for compliance string generation.

## Compliance honesty (compliance.py)
- CC8.1 / ISO 27001 / ISO 42001 / summary: corrected CLI-verifier
  descriptions. The CLI verifier is a regex/AST structural denylist, NOT
  Z3/SMT-proven. Unfounded claims dropped, not reworded.

## Tests
- pytest --co (2026-06-14): 504 collected = 495 passed + 9 skipped.
- New soundness suites: code / hw / distill / tool verifiers, offline (L6),
  attestation (L5), compliance, ops-scripts, docs-drift.

## Deploy
- Research server (89.167.66.225) was at c48d709 (v13.4.1) through this
  release. Fast-forward checkout, no divergence.
- Rollback: `git checkout v13.4.1`.
