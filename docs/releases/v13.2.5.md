# substrate-guard v13.2.5 — Ordered runbook (eBPF → Postgres → orchestrator)

## Documentation

- **[RUNBOOK_ORDERED.md](../RUNBOOK_ORDERED.md)** — index în ordine: roadmap → doctor → eBPF → Postgres/audit → integrare.
- **[DOCKER_EBPF.md](../DOCKER_EBPF.md)** — `monitor --live`, host Linux vs container privilegiat, limitele imaginii slim.
- **[DOCKER_POSTGRES_AUDIT.md](../DOCKER_POSTGRES_AUDIT.md)** — `docker-compose.stack.yml` pas cu pas.

## Scripts

- **`scripts/stack_audit.sh`** (Linux/macOS) — `up -d --build`, așteaptă Postgres, `doctor`, `audit`.
- **`scripts/stack_audit.ps1`** (Windows) — același flux.

## Example

- **`examples/orchestrator_inject_events.py`** — orchestrator fără eBPF: `inject_and_evaluate` + raport ASCII-safe pe consolă Windows.

## Tests

- **`tests/test_examples_orchestrator.py`** — rulează exemplul ca subprocess.

## Other

- **[DEPLOY.md](../DEPLOY.md)** — link către runbook la început.
