# Copy to .env when using docker-compose.guard.yml or docker-compose.stack.yml.
# For stack.yml, set at least:
# POSTGRES_PASSWORD=your-secure-password
#
# Optional overrides (defaults exist in compose):
# POSTGRES_USER=guard
# POSTGRES_DB=substrate
